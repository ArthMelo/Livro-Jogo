#include<stdio.h>
#include<string.h>
#include <stdlib.h>

void inicio()
{
    char texto[2000];
        FILE *arquivo;
        arquivo = fopen("arthur.txt","r");

        if(arquivo == NULL){
        printf("Impossivel rodar");
    }

    while(fscanf(arquivo, "%s", texto) != EOF){
        printf("%s ", texto);
    }
    fclose(arquivo);
}

void comecar()
{
    int i;
    printf("BEM-VINDO AO NEON NEXUS\n");
    system("pause");
    printf("Digite [1] para comecar um novo jogo\nDigite [2] para recomecar um jogo\nDigite [3] para sair\n");
    scanf("%d", &i);

    if(i == 2){
        printf("Nao vai comecar nao kkkk\n");
    }
    if(i == 1){
        inicio();
    } else {
        exit(1);
    }
}

void continuar()
{
    char texto[2000];
        FILE *arquivo;
        arquivo = fopen("inicio2.txt","r");

        if(arquivo == NULL){
        printf("Impossivel rodar");
    }

    while(fscanf(arquivo, "%s", texto) != EOF){
        printf("%s ", texto);
    }
    fclose(arquivo);
}

typedef struct PERSONAGEM_
{
    int vida;
    int lealdade;
    int defesa;
    int ataque;
    int itens[3];
} personagem;


void atributos(){
    printf("Voce ira controlar Hector, ele comeca com 100 de vida, 5 de ataque e 0 de defesa\n");
        system("pause");
    printf("Seus atributos aumentarao conforme suas escolhas e sua jornada\n");
        system("pause");
    printf("BOA SORTE!\n");
        system("pause");

}

int main(void)
{
    comecar();
    printf("\n");
    system("pause");
    continuar();
    printf("\n");
    system("pause");
    atributos();
    
    personagem Hector;

        Hector.vida = 100;
        Hector.defesa = 0;
        Hector.ataque = 5;

    
}
